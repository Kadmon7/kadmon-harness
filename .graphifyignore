# Only truly redundant / generated / self-referential files.
# KEEP INCLUDED (so graphify maps the harness architecture):
#   .claude/agents/   — 16 specialists + their skill chains
#   .claude/skills/   — 46 reusable domain skills
#   .claude/commands/ — 11 command orchestrations
#   .claude/rules/    — 19 conventions enforced by hooks
#   docs/decisions/   — ADRs (design rationale IS knowledge)
#   docs/plans/       — implementation plans
#   scripts/lib/      — runtime TypeScript
#   tests/lib/        — unit tests as behavior spec

# Instruction files Claude reads every session — extraction would be circular.
CLAUDE.md
AGENTS.md
.claude/settings.json
.claude/settings.local.json

# Generated artifacts (not knowledge).
dist/
node_modules/
coverage/
.husky/_/
*.tsbuildinfo

# Test fixtures and build scratch (not architecture).
tests/fixtures/
tests/build/

# Graphify's own output — NEVER self-ingest. Reading graph.json back in
# would build a graph-of-the-graph on each rebuild = recursion noise.
graphify-out/

# Lock files (massive, machine-reproducible, no semantic value).
package-lock.json

# Security — secrets and local DBs. graphify's auto-skip is directory-only,
# so root-level .env / *.db files leak into the graph without explicit patterns.
.env
.env.*
*.db
*.db-journal

# Personal Claude scratch and curated insights (gitignored — keep out of graph too).
.claude.local.md
docs/insights/
