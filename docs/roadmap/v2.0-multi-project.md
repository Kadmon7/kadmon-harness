# v2.0 — Multi-Proyecto y Aprendizaje Avanzado

> Estado: FUTURO
> Meta: El harness funciona sin friccion entre multiples proyectos con comportamiento inteligente
> Restructurado 2026-04-13: absorbio items de v1.1 que NO eran necesarios para empezar a usar el harness en cualquier proyecto. v1.1 quedo razor-focused (Sprint C 2 BLOCKERS + plan-003 + Sprint B /evolve Generate). Update 2026-04-14: Sprint C (ADR-007) y Sprint B (ADR-008) shippearon; v1.1 solo tiene plan-003 bootstrap pendiente. Ver [`v1.1-learning-system.md`](v1.1-learning-system.md) seccion "Items movidos a v2.0" para tabla de origen.

## Instincts activos (comportamiento, no solo informacion)

- [ ] **Sugerencias basadas en instincts** — "Usualmente corres /verify antes de commitear"
- [ ] **Evaluacion de patrones en tiempo real** — no solo batch en compact/stop
- [ ] **Instinct warnings en tiempo real** [moved from v1.1 Sprint D 2026-04-13] — avisar cuando violas un patron con confidence >= 0.7
- [ ] **Auto-promotion cross-project** [moved from v1.1 Sprint E 2026-04-13] — mismo instinct en 2+ proyectos con confidence >= 0.8 → scope global
- [ ] **Instinct decay** [moved from v1.1 Sprint E 2026-04-13] — sin refuerzo > 30 dias, confidence -= 0.02/semana

## Tarea actual persistente

- [ ] **Tracking multi-sesion** — una tarea que cruza 3+ sesiones mantiene contexto
- [ ] **current_task field en sessions** [moved from v1.1 Sprint D 2026-04-13] — capturar intencion de /abra-kdabra y agents
- [ ] **"Retomando: [descripcion]" en banner de session-start** [moved from v1.1 Sprint D 2026-04-13]
- [ ] **Memory highlights en banner** [moved from v1.1 Sprint D 2026-04-13] — 3 entradas mas relevantes de MEMORY.md al iniciar
- [ ] **konstruct worked example** [moved from v1.1 Sprint D 2026-04-13] — agregar ejemplo concreto del stack al agente konstruct para calibrar nivel de detalle en planes

## Summary semantico (no heuristico)

- [ ] **Reemplazar summary heuristico con LLM** — costo estimado: ~$0.01/sesion
- [ ] **Capturar decisiones tomadas** — no solo "edited 4 files"
- [ ] **Capturar blockers** — que fallo y como se resolvio
- [ ] **Distincion "que estaba haciendo" vs "que herramientas use"**

## Memoria compartida entre proyectos

- [ ] **Registro global de skills** — compartir skills entre proyectos sin duplicar
- [ ] **Propagacion de instincts cross-project** — filtrado por confidence
- [ ] **Templates de proyecto** — bootstrap .claude/ desde patrones del harness
- [ ] **Agentes globales vs project-scoped** — definir cuales aplican a todo
- [ ] **Import/export de instincts** [moved from v1.1 Sprint E 2026-04-13] — compartir libraries entre proyectos via JSON/SQLite dump

## Pattern-definitions extendidas

> [moved from v1.1 Sprint A 2026-04-13] — Sprint A original cubrio harness maintenance (12 patterns ADR-006). Estos subitems quedaron diferidos: son extension del sistema, no requisitos para distribuir.

- [ ] **Patrones de workflow avanzado**
  - "Antes de /chekpoint, revisar diff completo"
  - "Despues de crear skill, correr eval con test cases"
  - "Despues de 3+ sesiones tocando el mismo archivo, considerar refactor"
- [ ] **Patrones de seguridad** (necesitan ADR propio segun ADR-006)
  - "Deteccion de API keys expuestas en staged changes"
  - "Injection patterns en user input"
- [ ] **Patrones por stack** (cuando se usen los stacks)
  - Python: linting con ruff despues de editar .py, virtual envs, type hints
  - Supabase: verificar RLS despues de crear tabla, migraciones antes de deploy
  - React: verificar re-renders despues de cambiar state, state management patterns
- [ ] **API integration** — rate limits, retry logic, health checks
- [ ] **Hebrew/multilingue** — patrones especificos para texto hebreo/arameo en ToratNetz

## DB hardening + observabilidad secundaria

> [moved from v1.1 Sprint C 2026-04-13] — Sprint C original tenia ~14 items. Solo 2 (timestamp inversion + duration_ms NULL) bloqueaban plan-003 y se mantienen en v1.1. El resto es hygiene/secundario y vive aqui.

### DB hardening
- [ ] **Zod validation en insert boundaries** — validar hookName, eventType, agentType con schemas Zod en insertHookEvent/insertAgentInvocation
- [ ] **ON DELETE CASCADE en FK** — agregar a hook_events, agent_invocations, cost_events (hoy cascade es manual en deleteSession)
- [ ] **CHECK constraints adicionales** — `CHECK (blocked IN (0, 1))`, `CHECK (success IS NULL OR success IN (0, 1))`
- [ ] **LIMIT defensivo en stats queries** — agregar `LIMIT 100` a getHookEventStats y getAgentInvocationStats
- [ ] **SELECT columnas explicitas** — reemplazar `SELECT *` con columnas nombradas (pre-existente en todo state-store)

### Observabilidad
- [~] **Capturar outputs/errores de tools** — parcial: observe-post.js captura resultSnippet (Bash, 100 chars) y error messages en failures. Falta: captura completa para todos los tools
- [ ] **Deteccion de regresiones de performance** — O(n^2), queries lentas, memory spikes
- [ ] **Costo por feature/PR/branch** — saber cuanto cuesta cada feature
- [x] **Monitoreo de latencia de hooks** — hook_events table con duration_ms, exit_code, blocked (v0.4)
- [ ] **CHANGELOG auto-generado** — basado en commits convencionales y ADRs
- [x] **Metricas de efectividad de agentes** — agent_invocations table con agentType, durationMs, success (v0.4). Pendiente: dashboard web
- [ ] **Dashboard hook health desde DB** [moved from v1.1 Sprint C 2026-04-13] — getHookHealthRows query desde hook_events en lugar de JSONL efimero
- [ ] **Hook timeout monitoring** [moved from v1.1 Sprint C 2026-04-13] — alertar cuando hook excede budget de latencia (>500ms para no-lifecycle). Prerequisito cumplido: `hook_events.duration_ms` se poblando correctamente desde 2026-04-14 (Sprint C / ADR-007).
- [ ] **Validar estimacion de costos** [moved from v1.1 Sprint C 2026-04-13] — comparar cost_events estimados con uso real de tokens (cross-check semanal post-uso)
- [ ] **Politica de archivado de sesiones** [moved from v1.1 Sprint C 2026-04-13] — archivar o borrar sesiones > 90 dias (definir retention policy + comando `/forge archive-old-sessions` o hook de limpieza)

## Supabase sync (ADR-001 fase 2)

- [ ] **Sync de sessions, instincts, cost_events** a Supabase
- [ ] **Continuidad cross-dispositivo** — trabajar desde otra maquina con el mismo contexto
- [ ] **Backup en la nube** — reemplazar kadmon.db.bak local
- [ ] **Dashboard web** — analytics en browser, no solo CLI

## Skill library expansion (ECC import)

> [moved from v1.1 Sprint F 2026-04-13] — Tier S (8 meta-skills) y Tier A (16 skills general value) cerrados en v1.1 el 2026-04-13 — los 24 ya estan wireados a sus agents. Solo Tier B (project-gated) y Tier C (situacional) viven aqui.
> Metodologia: copy body, rewrite frontmatter (pushy/trigger-rich), add Integration section apuntando al agente del harness, add no_context Application. Aplicar rubrica skill-creator (>=8.5/10) antes de commit.
> Referencia: auditoria de skills 2026-04-13 + exploracion repo 181 skills (`affaan-m/everything-claude-code`).

### Tier B — ToratNetz / KAIRON (5, project-gated)

Activar cuando se use el proyecto correspondiente, no antes.

- [ ] iterative-retrieval → ToratNetz (RAG refinamiento progresivo)
- [ ] knowledge-ops → ToratNetz (KB management + ingestion)
- [ ] exa-search → ToratNetz (neural search via Exa MCP)
- [ ] backend-patterns → KAIRON (Node/Express API)
- [ ] dashboard-builder → Ambos (Grafana/SigNoz ops)

### Tier C — Situacional (7, no priorizar)

Solo si aparece un workflow concreto que las justifique: autonomous-loops, continuous-agent-loop, plankton-code-quality, santa-method, team-builder, agent-harness-construction, canary-watch.

## Hygiene backlog

> [moved from v1.1 "Backlog diferido" 2026-04-13] — items tacticos identificados durante /medik 2026-04-12 + audit 2026-04-13. Requieren evento disparador o decision arquitectonica antes de abordarse. Ninguno bloquea distribucion del harness.

- [ ] **ADR-004: gate por severidad en /medik** — hoy /medik para en gate todo-o-nada despues de Phase 2. Propuesta: auto-aplicar findings LOW/MEDIUM y solo gatear en HIGH/CRITICAL o cuando toquen archivos sensibles (package.json, schema.sql, settings.json). Requiere `/abra-kdabra` con arkitect. **NOTA**: con tiered /chekpoint dogfoodeado (2026-04-12), ~80% del valor de este ADR se cubre via tier split. Re-evaluar si sigue siendo urgente antes de invocar arkitect.
- [ ] **DEP0190 `shell: true` en migrate-v0.4.test.ts** — Node deprecation warning. Ubicacion: `tests/lib/migrate-v0.4.test.ts:109-111`. spektr verifico inputs hardcoded (sin vector de injection). Severidad LOW. Fix: reemplazar con `execFileSync("npx.cmd", [...], { shell: false })`.
- [ ] **tsconfig excluir `tests/` del build** — hoy tsc compila `tests/*.ts` → `dist/tests/*.js`, infla dist/. Causa raiz en tsconfig.json `include`. Requiere verificar que ningun production import desde `tests/`. Beneficio: dist/ mas pequeño, build mas rapido.
- [ ] **Limpieza de `$TEMP/kadmon/` (tests)** — diagnosticado 2026-04-13. ~2000+ directorios residuales (`test-mkdir-new-0..999`, `test-isolation-*`, `test-perf-*`, `test-isolate-*`). Fix doble: (a) `afterEach` con `fs.rmSync` en los tests que los crean; (b) script one-shot `scripts/cleanup-tmp-kadmon.ts`. No urgente.

## Proyectos destino
| Proyecto | Stack | Desafios unicos |
|----------|-------|-----------------|
| **ToratNetz** | Supabase + pgvector, Hebrew text, React | RAG multilingue, embeddings hebreo/arameo, textos antiguos |
| **KAIRON** | React Native, ElevenLabs voice, real-time | Voz en tiempo real, companion AI, estado emocional |
