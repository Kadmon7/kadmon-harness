# v1.0 — Listo para Produccion

> Estado: COMPLETADO
> Meta: El harness es confiable para construir proyectos reales encima (ToratNetz, KAIRON)
> Criterio de "listo": Sprint A + Sprint B completos, 0 hooks fallando silenciosamente
> Actual: 422 tests, 15 agentes, 22 skills, 12 comandos, 20 hooks, 19 reglas, 6 tablas DB

## Completado

- [x] Contexto git en session-start banner (ultimo commit, cambios sin commitear)
- [x] Pruning de instincts al iniciar sesion (pruneInstincts automatico)
- [x] Limpieza de dirs viejos en /tmp/kadmon/ (> 7 dias, validacion UUID)
- [x] Limpieza de observations.jsonl despues de persistir sesion
- [x] Sessions se cierran limpiamente en compact (sin phantom orphans)
- [x] Instincts se evaluan en crash recovery (orphan pattern evaluation)
- [x] compaction_count se preserva correctamente
- [x] Limpieza de test-isolation dirs via globalTeardown + age guard (8362694)
- [x] Tests para session-end-all.js — 8 tests (7a263ea)
- [x] Tests para pre-compact-save.js — 6 tests (7a263ea)
- [x] Tests para los 20 hooks — cobertura completa (e2f63ba)
- [x] REFERENCE.md consolidado en README (343c673)
- [x] Suite de tests: 180 -> 260 -> 328 -> 344 (37 archivos de test)
- [x] Agentes: 14 -> 15, skills: 20 -> 22
- [x] DB: hook_events + agent_invocations tables (v0.4 migration, 11 hooks wired, session-end extraction)

### Sprint A: Confiabilidad de hooks (completado)

- [x] **Auto-build antes de lifecycle hooks** — `ensure-dist.js` detecta staleness y corre `npm run build`
- [x] **Health check de dist/ al iniciar sesion** — warning en banner si dist/ esta stale despues del auto-build
- [x] **Deteccion de hooks fallidos** — `hook-logger.js` loguea errores a `~/.kadmon/hook-errors.log`
- [x] **Aceptacion: 0 hooks fallando silenciosamente** — verificado via hook-errors.log

### Sprint B: Limpieza de datos (completado)

- [x] **Limpiar sesiones de test** — `deleteSession()` + `cleanupTestSessions()` en state-store, script: `npx tsx scripts/cleanup-test-sessions.ts`
- [x] **Rotacion de backups** — `backup-rotate.js` mantiene 3 backups con timestamp, migra formato viejo `.bak`

## Acceptance criteria — validacion pendiente

El build de v1.0 esta completado. La validacion real requiere uso en un proyecto productivo.
Esta checklist no es build work; es el criterio de aceptacion que confirma que v1.0 funciona fuera del harness.

- [ ] **Feature real en ToratNetz** — al menos 1 feature completa (ej: Supabase schema o primer RAG endpoint)
- [ ] **Sesion completa sin workarounds** — iniciar, planificar, implementar, testear, commitear usando solo comandos del harness

> Esta validacion tambien aparece como `Prerequisito` en [`v1.1-learning-system.md`](v1.1-learning-system.md) — v1.1 no arranca hasta que v1.0 este validado en ToratNetz.

## Diferido a v1.1

Ver [`v1.1-learning-system.md`](v1.1-learning-system.md) — 5 sprints (A-E) con pattern-definitions mejorados, evolve pipeline, DB hardening + observabilidad, mejoras de sesion, y cross-project learning.

Los 3 items de monitoreo que estaban huerfanos aqui (hook timeout, validacion de costos, archivado de sesiones) ahora viven en v1.1 Sprint C (DB hardening + observabilidad).

## Eliminado (15 items)

Tests retroactivos de features estables (5): git context, /tmp cleanup, pruneInstincts,
  observations cleanup, endSession en pre-compact — estables hace semanas, ROI bajo.
Error SessionStart:compact (1): no ha recurrido desde la consolidacion de stop hooks.
Pipeline de promocion e2e (1): path no testeado pero no bloquea v1.0.
Dashboard vs instincts post-prune (1): verificacion manual de 30 segundos.
/akademy en agentes clave (1): util para optimizar, no para lanzar.
Verificacion de orchestration chain (1): funciona en practica, test formal innecesario.
Skeletons ToratNetz / KAIRON (2): eso es USAR v1.0, no construirlo. Fuera de alcance.
Hooks en otros directorios (1): probar cuando realmente se necesite.
Compartir agentes entre proyectos (1): copiar funciona, optimizar despues.
4 guias de documentacion (4): Windows, troubleshooting, performance, primer dia —
  la info ya existe en CLAUDE.md y rules/.
Rutas completas en summary (1): menor, solo importa en monorepos.
