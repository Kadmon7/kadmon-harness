# v1.0 — Production Ready

> Status: PLANNED
> Goal: Harness is reliable enough to build real projects on top (ToratNetz, KAIRON)

## Session continuity
- [x] Git context in session-start banner (last commit, uncommitted changes)
- [x] Instinct pruning on session-start (pruneInstincts called automatically)
- [x] Cleanup old /tmp/kadmon/ dirs (> 7 days, UUID-validated)
- [x] Cleanup observations.jsonl after session-end persistence
- [x] Sessions close cleanly on compact (no phantom orphans)
- [x] Instincts evaluated on crash recovery (orphan pattern evaluation)
- [ ] "Current task" persistence across sessions (field in sessions table or separate)
- [ ] Instinct contradiction warnings (warn when violating high-confidence pattern)

## Data quality
- [ ] Validate cost estimation accuracy against actual usage
- [ ] Session archival policy (> 90 days -> archive table or delete)
- [ ] Backup rotation (keep 3-5 timestamped backups, not just 1)
- [ ] Full file paths in session summary (not just basenames)

## Testing
- [ ] Tests for session-start git context enrichment
- [ ] Tests for /tmp cleanup (UUID validation, age filter)
- [ ] Tests for pruneInstincts integration in session-start
- [ ] Tests for session-end-persist cleanup

## Multi-project foundation
- [ ] Create `.claude/` skeleton for ToratNetz (agents, rules, skills)
- [ ] Create `.claude/` skeleton for KAIRON
- [ ] Document how to bootstrap a new project on top of the harness
- [ ] Test harness hooks work correctly in non-harness project directories

## Documentation
- [ ] "Getting started with a new project" guide
- [ ] Troubleshooting guide (common hook failures, DB corruption recovery)
- [ ] Performance tuning guide (hook latency, context budget)
