# v1.0 — Listo para Produccion

> Estado: EN PROGRESO
> Meta: El harness es confiable para construir proyectos reales encima (ToratNetz, KAIRON)
> Criterio de "listo": usar el harness en un feature real de ToratNetz sin que nada se rompa

## Continuidad entre sesiones

### Completado
- [x] Contexto git en session-start banner (ultimo commit, cambios sin commitear)
- [x] Pruning de instincts al iniciar sesion (pruneInstincts se llama automaticamente)
- [x] Limpieza de dirs viejos en /tmp/kadmon/ (> 7 dias, validacion UUID)
- [x] Limpieza de observations.jsonl despues de persistir sesion
- [x] Sessions se cierran limpiamente en compact (sin phantom orphans)
- [x] Instincts se evaluan en crash recovery (orphan pattern evaluation)
- [x] compaction_count se preserva correctamente

### Pendiente
- [ ] **Tarea actual persistente** — que la sesion recuerde QUE estabas haciendo, no solo que archivos tocaste
- [ ] **Warnings de instincts** — avisar cuando violas un patron con confidence >= 0.7 (ej: "Verify before committing")
- [ ] **Memory highlights en banner** — leer MEMORY.md y mostrar las 3 entradas mas relevantes al iniciar sesion
- [ ] **Investigar error SessionStart:compact** — el hook de session-start a veces falla despues de compact (visto 2026-03-30)

## Confiabilidad de hooks

- [ ] **Auto-build antes de lifecycle hooks** — si `dist/` esta desactualizado vs `scripts/lib/`, correr `npm run build` automaticamente. Hoy si editas TS y no compilas, los hooks fallan silenciosamente.
- [ ] **Deteccion de hooks fallidos** — hoy los hooks fallan con exit(0) y nadie se entera. Agregar logging de errores a un archivo persistente (`~/.kadmon/hook-errors.log`)
- [ ] **Health check de dist/** — al iniciar sesion, verificar que `dist/` existe y es mas reciente que `scripts/lib/`. Si no, warning en el banner.
- [ ] **Hook timeout monitoring** — detectar hooks que se pasan del budget de latencia (<50ms observe, <500ms otros)

## Calidad de datos

- [ ] **Validar estimacion de costos** — comparar con uso real cuando Claude Code envie datos de tokens
- [ ] **Politica de archivado de sesiones** — sesiones > 90 dias → archivar o borrar
- [ ] **Rotacion de backups** — mantener 3-5 backups con timestamp, no solo 1 (kadmon.db.bak)
- [ ] **Rutas completas en summary** — mostrar paths completos, no solo basenames (critico en monorepos)
- [ ] **Limpiar sesiones de test** — las 25+ sesiones "test-ses..." con 0 mensajes llenan la DB sin valor

## Instincts y patrones

- [ ] **Probar pipeline de promocion e2e** — los 10 instincts estan en 0.9 pero ninguno se ha promovido a skill. Nunca se ha validado que `/instinct promote` funciona de inicio a fin.
- [ ] **Expandir pattern-definitions.json** — solo hay 13 patrones (todos TS/Node). Agregar al menos 5 mas para cubrir git workflows, security, y database patterns.
- [ ] **Instinct decay** — instincts sin refuerzo > 30 dias deberian perder confidence gradualmente. Hoy se quedan en 0.9 para siempre.
- [ ] **Dashboard muestra instincts correctos** — verificar que `/dashboard` refleja los instincts despues del prune

## Testing

### Hooks sin tests (deuda critica)
- [ ] Tests para `pre-compact-save.js` (endSession, compactionCount, pattern eval)
- [ ] Tests para `cost-tracker.js` (estimacion, upsert, modelo correcto)
- [ ] Tests para `evaluate-session.js` (patron evaluation, instinct creation)
- [ ] Tests para `session-end-persist.js` (cleanup, summary, endSession)
- [ ] Tests para `session-end-marker.js` (clean-exit.marker)

### Tests de features nuevos (sesion 2026-03-30)
- [ ] Tests para git context en session-start (verificar "## Git Context" en output)
- [ ] Tests para /tmp cleanup (validacion UUID, filtro por edad, exclusion test-isolation)
- [ ] Tests para pruneInstincts en session-start
- [ ] Tests para cleanup de observations en session-end-persist
- [ ] Tests para endSession en pre-compact-save

### Validacion de agentes
- [ ] **Correr `/eval` en los agentes clave** — code-reviewer, planner, architect. Verificar que producen output util.
- [ ] **Medir si la orchestration chain funciona** — cuando invocas /code-review, realmente carga coding-standards y receiving-code-review?

## Deuda de documentacion

- [ ] **REFERENCE.md** — test counts stale (dice 101 tests, son 154), 2 hooks faltantes en tablas (session-end-marker, commit-quality)
- [ ] **REFERENCE.md** — seccion de audit directory desactualizada (falta harness-audit-2026-03-30.md)
- [ ] **REFERENCE.md** — MCP count inconsistente (seccion 10 dice 3, seccion 12 dice 2)
- [ ] **Audit actualizado** — el audit de 2026-03-30 se corrigio pero idealmente generar uno fresco con `/eval`

## Fundacion multi-proyecto

- [ ] **Skeleton .claude/ para ToratNetz** — agentes, rules, skills adaptados a Hebrew RAG + Supabase
- [ ] **Skeleton .claude/ para KAIRON** — agentes para React Native + ElevenLabs
- [x] **Guia "Como arrancar un proyecto nuevo"** — docs/BOOTSTRAP.md (paso a paso, flujo real, que copiar/personalizar)
- [ ] **Verificar hooks en otros directorios** — que los hooks del harness funcionen cuando trabajas en otro proyecto
- [ ] **Resolver compartir agentes entre proyectos** — hoy cada proyecto necesita su propio .claude/agents/. Investigar si se puede usar symlinks o una ubicacion global.

## Windows y entorno

- [ ] **Documentar gotchas de Windows** — `new URL().pathname` encodea espacios como %20, `npx tsx -e` no produce output, paths con espacios en `C:\Command Center\`
- [ ] **Guia de troubleshooting** — errores comunes de hooks, recuperacion de DB corrupta, como hacer rollback a kadmon.db.bak
- [ ] **Guia de performance** — latencia de hooks, presupuesto de contexto, cuando compactar
- [ ] **Guia de "primer dia"** — que leer, como orientarse, primer /dashboard, primer /kplan

## Validacion final (gate para v1.0)

- [ ] **Usar el harness en un feature real de ToratNetz** — al menos 1 feature completa (ej: setup de Supabase schema, o primer RAG endpoint). Si algo se rompe, es un bug de v1.0.
- [ ] **Sesion completa sin intervencion manual** — iniciar sesion, planificar, implementar, testear, commitear — todo usando comandos/agentes del harness, sin workarounds.
- [ ] **Todos los tests pasan** — 154+ tests, 0 failures
- [ ] **Ningun hook falla silenciosamente** — verificar con hook-errors.log
- [ ] **El roadmap v1.0 no tiene items criticos pendientes** — los items "nice to have" pueden quedar para v1.1
