node_modules/
# dist/scripts/ is committed (plugin distribution per ADR-010 Q1, enforced by
# .husky/pre-commit). Only dist/tests/ stays ignored — it's compiled test
# artifacts that have no production use.
dist/tests/
.husky/_/
.env
.env.*
*.log
.DS_Store
Thumbs.db
*.db
*.db-journal
# Python bytecode cache (generated by hook tests that run py_compile on fixtures)
__pycache__/
*.pyc
.claude.local.md
.claude/agent-memory/
# Personal overrides — NEVER commit (per Claude Code best practice)
.claude/settings.local.json
# Personal productivity journaling — private analytical notes, not for plugin distribution
docs/insights/
